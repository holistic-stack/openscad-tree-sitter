{
  "name": "@openscad/tree-sitter-openscad",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for OpenSCAD",
  "main": "bindings/node",
  "types": "bindings/node/index.d.ts",
  "browser": "bindings/web/index.js",
  "scripts": {
    "build": "nx exec -- tree-sitter generate && nx exec -- tree-sitter build --wasm",
    "build:node": "node-gyp rebuild",
    "install": "tree-sitter init && tree-sitter generate && node-gyp-build",
    "prebuildify": "prebuildify --napi --strip",
    "generate": "tree-sitter generate",
    "test": "nx exec -- tree-sitter test",
    "parse": "nx exec -- tree-sitter parse",
    "playground": "nx exec -- tree-sitter playground",
    "lint": "nx exec -- eslint ."
  },
  "keywords": [
    "parser",
    "tree-sitter",
    "openscad",
    "grammar",
    "syntax",
    "highlighting",
    "incremental",
    "parsing",
    "3d",
    "cad"
  ],
  "files": [
    "grammar.js",
    "queries/*",
    "src/**",
    "bindings/node/**",
    "bindings/web/**",
    "bindings/wasm/**",
    "binding.gyp",
    "prebuilds/**"
  ],
  "author": "OpenSCAD Community",
  "license": "MIT",
  "dependencies": {
    "node-gyp-build": "^4.8.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.25.3",
    "node-addon-api": "^8.3.1",
    "node-gyp": "^11.2.0",
    "prebuildify": "^6.0.0",
    "web-tree-sitter": "0.25.3"
  },
  "peerDependencies": {
    "tree-sitter": "0.22.4",
    "web-tree-sitter": "0.25.3"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    },
    "web-tree-sitter": {
      "optional": true
    }
  },
  "tree-sitter": [
    {
      "scope": "source.openscad",
      "file-types": [
        "scad"
      ],
      "highlights": [
        "queries/highlights.scm"
      ],
      "tags": [
        "queries/tags.scm"
      ]
    }
  ]
}
